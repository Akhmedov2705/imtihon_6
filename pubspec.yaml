name: audio_example
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  flutter_bloc: ^9.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  just_audio: ^0.10.4
  shared_preferences: ^2.2.2
  provider: ^6.1.1

flutter:
  uses-material-design: true

  assets:
    - assets/audio/
    - assets/images/
